package com.training.solid;

//This implies Objects or entities should be open for extension but closed for modification by introducing an abstraction (using interface).
//This also means that you can extend the functionality without modifying the code
public class OpenClosedPrinciple {
}
